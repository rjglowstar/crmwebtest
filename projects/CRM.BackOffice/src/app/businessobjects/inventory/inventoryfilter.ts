export class InventoryFilter {

    // InvItems
    stoneId!: string
    certificateNo!: string
    kapan!: string[]
    shape!: string[]
    minSize!: number
    maxSize!: number
    color!: string[]
    clarity!: string[]
    cut!: string[]
    polish!: string[]
    symmetry!: string[]
    fluorescence!: string[]

    constructor() { }
}